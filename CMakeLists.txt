CMAKE_MINIMUM_REQUIRED(VERSION 3.31)
PROJECT(STOC_PROC LANGUAGES CXX)

FIND_PACKAGE(implot REQUIRED)

ADD_EXECUTABLE(STOC_PROC
    src/RandomWalk.cc 
    src/main.cc 
    src/PoissonProc.cc
)

TARGET_INCLUDE_DIRECTORIES(STOC_PROC PRIVATE src)
TARGET_LINK_LIBRARIES(STOC_PROC implot::implot)